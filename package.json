{
  "name": "yanera",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "cd apps/web && bun run dev",
    "dev:worker": "bun --watch --env-file=apps/worker/.env.local run apps/worker/src/index.ts",
    "dev:proxy": "cd apps/proxy && bun run dev",
    "dev:convex": "cd packages/database && bun run dev",
    "dev": "concurrently 'bun run dev:convex' 'bun run dev:web' 'bun run dev:proxy' 'bun run dev:worker'"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
